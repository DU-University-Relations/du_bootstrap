<div class="columns small-12 medium-4">
    <div class="stories-module__story">
        <div class="content" data-equalizer-watch="row">
            {% block story %}
                {{ story.src }}
                {% include "@du_bootstrap/navigation/headline.twig" with story %}
                {% include "@du_bootstrap/paragraph/paragraph.twig" with story %}
            {% endblock %}
        </div>
        <div class="story-cta">
            {% block button %}
                {% if(story.button.title) %}
                    <p>{% include "@du_bootstrap/misc/button.twig" with story.button %}</p>
                {% endif %}
            {% endblock %}
        </div>
    </div>
</div>
